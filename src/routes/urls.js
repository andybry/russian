export const lemmas = (pageNumber) => `/lemmas/p${pageNumber}`
export const lemma = (name) => `/lemmas/${name}`
export const forms = (pageNumber) => `/forms/p${pageNumber}`
export const lemmasWithPart = (part, pageNumber) => `/lemmas/${part}/p${pageNumber}`
